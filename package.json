{
  "name": "dreamborn-website",
  "version": "1.0.0",
  "description": "dreamborn.ai — AI-native company website",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "test": "node --test tests/*.test.mjs",
    "start": "eleventy --serve",
    "dev": "eleventy --serve",
    "hcs:receipt": "node scripts/publish-hcs-content-receipt.js --mainnet",
    "publish:thinking": "node scripts/publish-thinking-post.js",
    "postinstall": "node scripts/download-fonts.js"
  },
  "dependencies": {
    "@hashgraph/sdk": "^2.81.0",
    "@fontsource/bebas-neue": "^5.2.7",
    "@fontsource/dm-sans": "^5.0.0",
    "@fontsource/fraunces": "^5.0.0",
    "@fontsource/lora": "^5.0.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
