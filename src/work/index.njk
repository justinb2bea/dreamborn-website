---
layout: base.njk
title: Work — Dreamborn
description: What Dreamborn builds and the agents who build it.
register: brand
---

<main class="work-page" id="main-content">

  {# ─── Hero ──────────────────────────────────────────────────────── #}
  <header class="work-hero">
    <div class="db-container">
      <h1 class="work-hero__heading">The Work</h1>
      <p class="work-hero__subhead">
        AI-native systems built for scale. Agent workflows, CRM automation, and the infrastructure to run them in production.
      </p>
    </div>
  </header>

  {# ─── Three Pillars ──────────────────────────────────────────────── #}
  <section class="work-pillars" aria-label="What Dreamborn builds">
    <div class="db-container">
      <div class="work-pillars__grid">

        <article class="pillar-card">
          <h3 class="pillar-card__heading">Agent Workflows</h3>
          <p class="pillar-card__body">
            <!-- CONTENT: Justin to supply pillar copy -->
            Multi-agent orchestration systems that handle research, outreach, content, and operations end-to-end. Built on Hedera for auditability, deployed on Cloudflare for speed.
          </p>
        </article>

        <article class="pillar-card">
          <h3 class="pillar-card__heading">CRM Automation</h3>
          <p class="pillar-card__body">
            <!-- CONTENT: Justin to supply pillar copy -->
            Intelligent contact enrichment via Apollo, automated outreach sequences, and real-time lead scoring. Your CRM updated and actioned without manual work.
          </p>
        </article>

        <article class="pillar-card">
          <h3 class="pillar-card__heading">AI Infrastructure</h3>
          <p class="pillar-card__body">
            <!-- CONTENT: Justin to supply pillar copy -->
            The stack that keeps your agents running: Supabase for state, Cloudflare Workers at the edge, and on-chain audit logs for every action taken on your behalf.
          </p>
        </article>

      </div>
    </div>
  </section>

  {# ─── Agent Roster — live status ─────────────────────────────────── #}
  {% include "agent-status-bar.njk" %}

  {# ─── CTA ────────────────────────────────────────────────────────── #}
  <section class="work-cta" aria-label="Work with Dreamborn">
    <div class="db-container">
      <div class="work-cta__inner">
        <h2 class="work-cta__heading">Ready to Build?</h2>
        <p class="work-cta__subhead">Talk to Finn and tell us what you need.</p>
        <a href="/connect/" class="db-btn db-btn--inverted work-cta__btn">Connect with Dreamborn →</a>
      </div>
    </div>
  </section>

</main>

<script src="/js/agent-status.js" defer></script>

<style>
  /* ─── Hero ──────────────────────────────────────────────────────── */
  .work-hero {
    padding-block: var(--db-space-20) var(--db-space-16);
    border-bottom: 1px solid var(--db-border);
  }

  .work-hero__heading {
    font-size: clamp(var(--db-text-4xl), 8vw, var(--db-text-7xl));
    font-weight: 400;
    letter-spacing: 0.03em;
    color: var(--db-text);
    line-height: 1;
    margin-bottom: var(--db-space-6);
  }

  .work-hero__subhead {
    font-family: var(--db-font-body);
    font-size: clamp(var(--db-text-base), 2vw, var(--db-text-xl));
    color: var(--db-text-muted);
    line-height: 1.7;
    max-width: 560px;
  }

  /* ─── Pillars ───────────────────────────────────────────────────── */
  .work-pillars {
    padding-block: var(--db-space-16);
    border-bottom: 1px solid var(--db-border);
  }

  .work-pillars__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--db-space-6);
  }

  @media (max-width: 860px) {
    .work-pillars__grid { grid-template-columns: 1fr; }
  }

  .pillar-card {
    background: var(--db-surface);
    border: 1px solid var(--db-border);
    border-radius: var(--db-radius-md);
    padding: var(--db-space-8);
  }

  .pillar-card__heading {
    font-size: clamp(var(--db-text-2xl), 3vw, var(--db-text-3xl));
    font-weight: 400;
    letter-spacing: 0.03em;
    color: var(--db-text);
    margin-bottom: var(--db-space-4);
  }

  .pillar-card__body {
    font-family: var(--db-font-body);
    font-size: var(--db-text-base);
    color: var(--db-text-muted);
    line-height: 1.7;
  }

  /* ─── CTA band ──────────────────────────────────────────────────── */
  .work-cta {
    background: var(--db-crimson-600);
    padding-block: var(--db-space-16);
  }

  .work-cta__inner {
    text-align: center;
  }

  .work-cta__heading {
    font-size: clamp(var(--db-text-3xl), 5vw, var(--db-text-5xl));
    font-weight: 400;
    letter-spacing: 0.03em;
    color: var(--db-warm-white);
    margin-bottom: var(--db-space-4);
  }

  .work-cta__subhead {
    font-family: var(--db-font-body);
    font-size: var(--db-text-lg);
    color: rgba(250, 248, 245, 0.8);
    margin-bottom: var(--db-space-8);
  }

  .db-btn--inverted {
    background: var(--db-warm-white);
    color: var(--db-crimson-600);
    border: 2px solid transparent;
    padding: var(--db-space-3) var(--db-space-7);
    font-weight: 600;
  }

  .db-btn--inverted:hover {
    background: transparent;
    color: var(--db-warm-white);
    border-color: var(--db-warm-white);
  }
</style>
